%section#breadcrumb
  %ul.breadcrumb
    %li
      %a(href="#") ホーム
    %li
      %a(href="#") 検索結果
    %li#last_bread_2
      %a(href="#") 　

%section#loader

%section#result.grid
  %h1.hide SEARCH RESULT
  - @result['items'].each do |res|
    - res = res['volumeInfo']
    - isbn = res['industryIdentifiers'][0]['identifier'] if res['industryIdentifiers']
    .col_2
      %div
        %div
          - if res['imageLinks'].nil?
            %div.no_img No Image..
          - else
            - img_link = res['imageLinks']['thumbnail']
            %img(src="#{img_link}")
        %p
          %strong #{res["title"]}
          %br
          (#{res["authors"].to_a.join(',') || "著者名なし"})

      %div.left.notice.warning
        %i.icon-spinner.icon-large
        %span 在庫検索中..

      %div.book_data.hide
        %ul
          %li.title #{res['title']}
          %li.authors #{res['authors']}
          %li.img_link #{img_link}
          %li.publisher #{res['publisher']}
          %li.isbn #{isbn}
          %li.stock

      %div
        - if isbn && isbn.length == 10
          %a(href="/detail/#{isbn}" class="button pjax")
            %i(class="icon-zoom-in icon-large")
            詳細を見る
        - else
          %a(href="#" class="button disabled")
            %i(class="icon-zoom-in icon-large")
            詳細情報なし
